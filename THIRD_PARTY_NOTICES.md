# Third-party notices

Agent Lifecycle Kit is distributed under Apache-2.0.

The current core implementation uses the Python standard library at runtime.
Development and packaging metadata may reference tooling declared in
`pyproject.toml` and `uv.lock`; downstream redistributors should review those
files for their exact dependency graph.
