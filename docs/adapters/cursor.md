# Cursor adapter

The Cursor projection packages shared lifecycle skills and Cursor adapter
metadata. The adapter is experimental until live Cursor conformance evidence is
published in a release support matrix.

The lifecycle core remains outside Cursor-specific prompt text. Cursor-specific
integration should only translate invocation, discovery and approval surfaces.
