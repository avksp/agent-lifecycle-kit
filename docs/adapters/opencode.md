# OpenCode adapter

The OpenCode projection packages shared lifecycle skills and an OpenCode
adapter. The adapter is experimental until live OpenCode conformance evidence
is published in a release support matrix.

OpenCode-specific code must not reimplement lifecycle planning, freeze,
workflow, review or final-audit semantics.
