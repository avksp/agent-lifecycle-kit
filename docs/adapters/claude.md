# Claude Code adapter

The Claude Code projection packages the shared lifecycle skills and Claude
adapter metadata. The adapter is experimental until live Claude Code
conformance evidence is published in a release support matrix.

For local development, load the repository as a local plugin only after the
Claude manifest is present in a release candidate.
