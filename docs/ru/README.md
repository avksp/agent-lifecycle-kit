<p align="center">
  <img src="assets/agent-lifecycle-kit-banner.svg?v=2026-08-02-1" alt="Agent Lifecycle Kit - SDD, SDLC и проверяемое завершение работы агента" width="100%">
</p>

# Agent Lifecycle Kit

Agent Lifecycle Kit (ALK) задаёт единый процесс работы для кодовых агентов. Он
помогает провести задачу от запроса до проверяемого результата: сначала
спецификация и план, затем работа в заданных границах, проверка реализации и
финальное подтверждение.

Проект работает поверх разных CLI: Codex, Claude Code, Qwen Code, Goose,
OpenInterpreter, Pi, Grok Build и других. Ядро не зависит от конкретного
провайдера или модели; команды, настройки окружения и работа с секретами
выносятся в адаптеры или локальные профили хоста.

Главная цель проекта - закрывать задачу полностью, с максимально возможным
качеством для выбранной модели, без оверинжиринга и с контролем расхода токенов.

**Лицензия:** Apache-2.0 · **Версия:** 1.29.0 · **Python:** 3.11-3.13

## Что даёт ALK

- Процесс ориентирован на результат: план, выполнение, проверка и подтверждение
  завершения.
- Один подход для разных CLI: особенности конкретного инструмента остаются в
  адаптерах, а новые адаптеры можно добавлять без изменения схем жизненного
  цикла.
- Маленькие и локальные модели получают компактный контекст, понятный следующий
  шаг и повторяемые проверки.
- Дополнительные профили включаются только тогда, когда их требует тип задачи
  или уровень риска.
- Учёт расхода показывает токены, время и ресурсы; денежная стоимость
  необязательна и используется только если её сообщает платный хост.

## Области возможностей

### Планирование и выполнение

- Спецификация и план проверяются до начала изменений в коде.
- Работа делится на пакеты с владельцем, разрешёнными файлами и критериями
  приёмки.
- Выполнение, проверка завершения, блокировки, повторные попытки и итоговое
  подтверждение фиксируются в структурированных артефактах.
- Черновые шаблоны задач покрывают исправление ошибок, путь от идеи до PR,
  проверку PR, исправление конфликтов слияния и подготовку релиза.

### Качество и подтверждение

- Аудит реализации сравнивает результат с зафиксированным планом и
  подтверждениями приёмки.
- Дополнительный профиль расследования ошибок (`Bug Forensics`) для задач по
  дефектам и регрессиям фиксирует воспроизведение до правки, устойчивый
  отпечаток ошибки, журнал гипотез, минимальную правку и регрессионную проверку.
- Контроль целостности для рискованных финальных подтверждений хранит
  устойчивые выводы проверки, отпечатки причины, влияние правки и цепочку хэшей.
- Дополнительная перепроверка, правила запуска и подтверждения записи обратно
  выключены по умолчанию и становятся обязательными только если это явно задано
  в плане.

### Маршрутизация и расход

- Компактные пакеты для маленьких моделей, краткие профили контекста, снимки
  цели и локальная статистика качества помогают выбрать самый лёгкий безопасный
  режим.
- Измерения по этапам фиксируют токены, длительность и счётчики ресурсов без
  обязательной денежной оценки.
- Экспорт использования показывает сессии, токены, ресурсы, отпечатки
  подтверждений и необязательный `cost_usd`, если его сообщает хост.

### Адаптеры и импорт

- Адаптеры отделяют команды, настройки и ограничения конкретного CLI от схем
  жизненного цикла.
- Новый CLI подключается через дескриптор, правила запуска, границу окружения и
  отдельные подтверждения проверки; ядро при этом не меняется.
- Проверки возможностей адаптера строят ограниченные планы пробных запусков и
  сравнивают реальные подтверждения без автоматического изменения статуса.
- Импорт внешних процессов, форматов агентов и тикетов используется только как
  черновой вход для последующей проверки.
- Поиск по прошлым эпизодам работает с краткими резюме подтверждений и сессий,
  сохраняя признак проверенной или непроверенной цепочки.

### Операции

- Артефакты восстановления запуска покрывают снимок попытки, восстановление,
  отказ от попытки, выбранную попытку, закрепление исполнителя и сигнал
  активности.
- Артефакты песочницы для рискованных задач описывают ограничения среды запуска
  отдельно от разрешённых путей записи в Git.
- Диагностика готовности, лента событий и представление прогресса по умолчанию
  не меняют состояние и не запускают реальные модельные вызовы.

## Быстрый старт

Из исходного дерева:

```bash
python -m pip install -e .
agent-lifecycle version
agent-lifecycle diagnose --no-install-plans
agent-lifecycle adapter validate --descriptor adapters/codex/adapter.descriptor.json
```

Пошаговый пример: [Быстрый старт](quickstart.md).

## Обычный цикл

Обычный путь: спецификация -> зафиксированный план -> работа в заданных
границах -> проверка реализации -> подтверждение результата. Основные команды
покрывают спецификацию, план, рабочий цикл, аудит, адаптеры, импорт, метрики,
правила, диагностику и состояние запуска. Подробности:
[Справочник команд](reference/cli.md) и [источник правды](reference/source-of-truth.md).

## Зрелость адаптеров

`EXPERIMENTAL` означает, что адаптер описан в исходниках и проходит
повторяемые локальные проверки. `VERIFIED` означает, что для конкретного CLI,
версии и локальной связки провайдера с моделью есть ограниченная проверка на
реальном хосте, калибровки расхода, принятое обезличенное резюме подтверждений
и финальное подтверждение жизненного цикла. Новые адаптеры можно добавлять
отдельно от ядра: сначала как `EXPERIMENTAL`, затем как `VERIFIED` только для
проверенного диапазона.

| Хост | Текущее заявление |
| --- | --- |
| Codex | `VERIFIED` для Codex CLI 0.145.0. Одобрение публичного каталога плагинов не заявлено. |
| Claude Code | `VERIFIED` для Claude Code 2.1.220. Одобрение официального каталога не заявлено. |
| OpenCode | `VERIFIED` для OpenCode CLI 1.18.9. Публикация в npm не заявлена. |
| Hermes | `VERIFIED` для Hermes Agent v0.19.0. Одобрение публичного каталога не заявлено. |
| Qwen Code | `VERIFIED` для Qwen Code 0.21.0 на проверенной локальной связке провайдера и модели. Одобрение публичного пакета не заявлено. |
| Cursor | `EXPERIMENTAL`; безопасный локальный осмотр прошёл, но подтверждений из реального запуска недостаточно. |
| Gemini CLI | `EXPERIMENTAL`; локальная проверка на реальном вызове ограничена текущим уровнем Gemini Code Assist. |
| Goose | `VERIFIED` для Goose 1.45.0 на проверенной локальной связке провайдера и модели. Одобрение публичного каталога не заявлено. |
| Kimi Code | `EXPERIMENTAL`; для проверки нужен настроенный провайдер и псевдоним модели. |
| Grok Build | `VERIFIED` для Grok Build 0.2.117 на проверенной локальной связке провайдера и модели. Одобрение публичного каталога не заявлено. |
| OpenInterpreter | `VERIFIED` для `interpreter` 0.0.34 на проверенной локальной связке провайдера и модели. Одобрение публичного каталога не заявлено. |
| Pi | `VERIFIED` для Pi 0.83.0 на проверенной локальной связке провайдера и модели. Одобрение публичного каталога не заявлено. |

Подробнее: [Установка адаптеров](adapters/install.md) и
[матрица поддержки адаптеров](adapters/support-matrix.md).

## Карта контрактов

Публичная поверхность жизненного цикла описана схемами. Полный список
стабильных идентификаторов схем, правила совместимости, артефакты
восстановления запуска, контракты перепроверки, контракты расследования ошибок
и детали экспорта использования находятся в
[Публичных контрактах](reference/public-contracts.md).

## Границы проекта

- Ядро не зависит от конкретного провайдера. Команды хоста и выбор модели
  живут в адаптерах или локальных профилях.
- Новые CLI подключаются через адаптеры: дескриптор, правила запуска, граница
  окружения и подтверждения проверки. Схемы жизненного цикла при этом остаются
  стабильными.
- Маленькие модели получают компактные пакеты, детерминированные проверки и
  явные следующие действия.
- Большие модели проходят те же обязательные проверки качества; более сильное
  рассуждение не заменяет подтверждающие артефакты.
- Публичные заявления релиза опираются только на отслеживаемые файлы и
  обезличенные резюме подтверждений.
- Git write-scope ограничивает пути репозитория; артефакты песочницы описывают
  ограничения среды запуска и могут оставаться `UNKNOWN` до отдельной проверки.
- Импорт внешних форматов и найденные прошлые эпизоды помогают с контекстом, но
  не заменяют проверенные исходные артефакты ALK.
- Дополнительная перепроверка, правила запуска и подтверждения записи обратно
  добавляют подтверждения только при запросе задачи или плана; это не режим
  одновременного запуска нескольких моделей по умолчанию.

## Документы

- Старт: [Быстрый старт](quickstart.md) и [черновик спецификации из issue](issue-to-spec.md).
- Адаптеры: [Установка адаптеров](adapters/install.md) и [матрица поддержки адаптеров](adapters/support-matrix.md).
- Справочник: [Команды CLI](reference/cli.md), [источник правды](reference/source-of-truth.md), [публичные контракты](reference/public-contracts.md) и [диагностика готовности](reference/readiness-diagnostics.md).
- Качество и ресурсы: [компактные пакеты для маленьких моделей](reference/small-model-packets.md), [адаптивные правила](reference/adaptive-lifecycle-policy.md), [локальная статистика качества](reference/quality-cost-learning.md), [учёт расхода жизненного цикла](reference/lifecycle-cost.md), [экспорт использования](reference/usage-export.md) и [целостность подтверждений](reference/evidence-integrity.md).
- Профили и операции: [представления статуса без записи](reference/read-only-status-view.md), [границы песочницы](reference/sandbox-boundaries.md), [импорт внешних форматов](reference/import-mappers.md), [поиск по прошлым эпизодам](reference/episode-retrieval.md), [восстановление запуска](reference/runner-recovery.md), [профиль перепроверки](reference/cross-check-profile.md), [профиль расследования ошибок](reference/bug-forensics.md) и [бюджет контекста для расследования ошибок](reference/bug-forensics-context-budget.md).
- Релизные материалы: [шаблоны задач](reference/task-templates.md) и [безопасность релиза](security/release-security.md).

## Лицензия

Apache-2.0. Текст лицензии находится в корне репозитория.
