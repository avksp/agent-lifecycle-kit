# Матрица поддержки адаптеров

Матрица фиксирует заявление исходного дерева о поддержке адаптеров. Она не
заменяет локальные проверки: перед использованием нужно запускать `diagnose`,
`adapter validate` и `adapter inspect`.

Различия между подключением ALK внутри внешнего инструмента и запуском
контроллера из терминала проекта описаны в разделе [использование ALK с
адаптером](usage-modes.md).

Поддержка управляемых сессий отделена от зрелости адаптера. Текущие встроенные
адаптеры объявляют `managedLaunch.status: WRAPPER_ONLY`: ALK может связать
работу с подтверждением жизненного цикла через управляемые команды или
обёртки, но не заявляет безопасный прямой argv-запуск CLI. Подробнее:
[управляемые сессии адаптеров](managed-session-support.md).
Любой общий запрос запуска через дескриптор блокируется кодом
`adapter-generic-launch-disabled` до создания процесса хоста; статус
дескриптора не может обойти эту границу.

Пользователь может отдельно включить проверенный локальный маршрут через
профиль в `.alk/host-launch/`. Он требует зафиксированные привязки и профиль
риска и возвращает локальное подтверждение, но не меняет строки таблицы ниже.
Подробнее: [локальный запуск внешней команды](../reference/local-host-launch.md).

Для всех двенадцати встроенных адаптеров предусмотрены локальные профили с
точной привязкой к версии. Они позволяют воспроизводимо создать профиль и
проверить версию, но не разрешают общий запуск через дескриптор и не являются
публичным заявлением поддержки. Профили требуют совпадающее локальное
подтверждение и сохраняют `WRAPPER_ONLY`. Подробнее:
[квалифицированный запуск внешнего
инструмента](../reference/qualified-host-launch.md).

Для запуска только планирования действует отдельный, более строгий статус. Все
двенадцать адаптеров имеют результат для точной версии. Codex `0.147.0`, Claude
Code `2.1.226`, Gemini CLI `0.46.0` и Goose `1.45.0` являются статическими
кандидатами; остальные восемь профилей явно не поддерживаются. Сейчас каждый
адаптер имеет состояние `PLANNING_ONLY_UNSUPPORTED`, поскольку в поставке нет
принятого подтверждения ограничений для точной версии. Зрелость `VERIFIED` не
может повысить этот статус. Подробнее: [запуск адаптера только для
планирования](../reference/planning-only-launch.md).

Локальный учёт токенов имеет отдельный статус. Claude Code, Codex, Gemini CLI,
Kimi Code, OpenCode и Qwen Code объявляют
`usageNormalization.status: FIXTURE_ONLY`: их ограниченные
нормализаторы проверены на тестовых образцах, но результаты ещё не подходят для
гейтов S1/S2. Для остальных встроенных дескрипторов контракт не объявлен, что
равнозначно `UNSUPPORTED`. Зрелость Qwen Code остаётся `VERIFIED`, но сама по
себе не подтверждает новый нормализатор. Подробнее:
[локальный учёт токенов хоста](../reference/host-local-token-accounting.md).

| Хост | Зрелость | Управляемый запуск | Что это значит |
| --- | --- | --- | --- |
| [Codex](codex.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Codex CLI 0.145.0. |
| [Claude Code](claude.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Claude Code 2.1.220. |
| [OpenCode](opencode.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для OpenCode CLI 1.18.9. |
| [Hermes](hermes.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Hermes Agent v0.19.0. |
| [Qwen Code](qwen-code.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Qwen Code 0.21.0 на локальной связке провайдера и модели. |
| [Cursor](cursor.md) | `EXPERIMENTAL` | `WRAPPER_ONLY` | Локальный осмотр проходит, но подтверждений из реального запуска недостаточно. |
| [Gemini CLI](gemini-cli.md) | `EXPERIMENTAL` | `WRAPPER_ONLY` | Проверка реальным вызовом ограничена текущим уровнем Gemini Code Assist. |
| [Goose](goose.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Goose 1.45.0 на локальной связке провайдера и модели; одобрение публичного каталога не заявлено. |
| [Kimi Code](kimi-code.md) | `EXPERIMENTAL` | `WRAPPER_ONLY` | Нужен настроенный провайдер и алиас модели. |
| [Grok Build](grok-build.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Grok Build 0.2.117 на локальной связке провайдера и модели; одобрение публичного каталога не заявлено. |
| [OpenInterpreter](openinterpreter.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для `interpreter` 0.0.34 на локальной связке провайдера и модели; реальная проверка совместимости, калибровка, ограничения среды и подтверждение жизненного цикла прошли локально, одобрение публичного каталога не заявлено. |
| [Pi](pi.md) | `VERIFIED` | `WRAPPER_ONLY` | Проверен для Pi 0.83.0 на локальной связке провайдера и модели; реальная проверка совместимости, калибровка, ограничения среды, проверка env-файла и подтверждение жизненного цикла прошли локально, одобрение публичного каталога не заявлено. |

`VERIFIED` относится только к указанному диапазону хоста и не означает
публичное одобрение каталога, npm-публикацию или готовность других версий.

## Захват событий

Дескрипторы и манифесты возможностей объявляют `adapter-event-stream` через
переносимую схему `agent-adapter-event.v1`. Офлайн-проверки требуют
`agent-adapter-event-stream-receipt.v1`, который связывает поток событий с
отпечатком дескриптора. Это проверка качества артефактов, а не повышение
зрелости адаптера.

Автоматическая установка: нет. Захват событий имеет границу `adapter-owned`.
Ядро ALK не устанавливает hook хоста, не пишет конфигурацию хоста и не
разбирает сырую телеметрию. Состояние hook, маршрут через обёртку, маршрут
артефакта и статус автоматической установки описаны в
[матрице захвата событий адаптеров](event-capture-matrix.md).

Подтверждения проверки возможностей — это детектор расхождений для реальной
совместимости. План строится из `agent-adapter-capability-manifest.v1`, не
запускает реальные вызовы, не меняет зрелость и не заявляет промышленную
готовность. Проверка подтверждений сравнивает артефакты реального хоста с
планом и падает при пропущенной запланированной операции, синтетическом
повторе для операции, требующей реального запуска, или обходе оболочки
host-protocol.

Подтверждения песочницы используют только `agent-sandbox-receipt.v1`. Частичные
ограничения дерева процессов и границы прокси для учётных данных записываются в
деталях артефакта; значения секретов и полные пути к приватным env-файлам
недопустимы.
