# Запуск зафиксированной задачи через проверенный профиль

ALK предоставляет дополнительный маршрут запуска зафиксированной задачи через
проверенный профиль внешнего инструмента. Проверенный профиль связывает точную
версию команды, параметры процесса, отпечаток профиля и подтверждение
предварительной проверки с задачей и доказательствами жизненного цикла. Выбор
провайдера и модели остаётся в настройках хоста.

В технических схемах и подтверждениях это состояние называется `qualified`.
Для пользователя это означает «проверенный профиль», а не отдельный выбор
провайдера или модели.

Раздел описывает реализацию уже проверенной и зафиксированной задачи. Для
планирования используется отдельный процесс из раздела [запуск адаптера только
для планирования](planning-only-launch.md).

| Адаптер | Проверенная версия | Исполняемый файл |
| --- | --- | --- |
| [Codex](../adapters/codex.md) | `0.147.0` | `codex` |
| [Claude Code](../adapters/claude.md) | `2.1.226` | `claude` |
| [OpenCode](../adapters/opencode.md) | `1.18.15` | `opencode` |

В дескрипторах адаптеров сохраняется
`managedLaunch.status: WRAPPER_ONLY`. Подтверждение относится только к точному
локальному профилю и установленной версии.

## Проверенные профили

У всех двенадцати встроенных адаптеров есть локальное описание точной версии.
Поэтому для каждого можно создать профиль, проверить его структуру и выполнить
ограниченную проверку версии. Это более широкая возможность, чем запуск
зафиксированной задачи через проверенный профиль.

Для трёх адаптеров из основной таблицы предусмотрены специальные профили
продолжения зафиксированной задачи, испытательные обвязки и нормализаторы
расхода этого маршрута. Ниже приведены профили остальных адаптеров с их
текущим статусом планирования:

| Адаптер | Заявленная версия | Исполняемый файл | Текущая граница |
| --- | --- | --- | --- |
| [Cursor](../adapters/cursor.md) | `2026.07.23` | `cursor-agent` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [Gemini CLI](../adapters/gemini-cli.md) | `0.46.0` | `gemini` | Статический кандидат без принятого подтверждения ограничений среды. |
| [Goose](../adapters/goose.md) | `1.45.0` | `goose` | Статический кандидат без принятого подтверждения ограничений среды. |
| [Grok Build](../adapters/grok-build.md) | `0.2.118` | `agent` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [Hermes](../adapters/hermes.md) | `0.19.0` | `hermes` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [Kimi Code](../adapters/kimi-code.md) | `0.30.0` | `kimi` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [OpenInterpreter](../adapters/openinterpreter.md) | `0.0.34` | `interpreter` | Безопасное описание проверки версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [Pi](../adapters/pi.md) | `0.83.0` | `pi` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |
| [Qwen Code](../adapters/qwen-code.md) | `0.21.8` | `qwen` | Описание точной версии; профиль планирования имеет состояние `UNSUPPORTED`. |

Для каждой строки значения `qualifiedLaunch.publicSupportClaimed` и
`productionPromotionClaimed` записываются как `false`. Успешная проверка
`--version` подтверждает соответствие профиля и версии; принятое доказательство
реализации формируется полной последовательностью квалификации выбранного
адаптера.

## Создание локального профиля

Выполните команду в целевом проекте. В `--repository-root` укажите исходный
каталог или каталог подключаемого модуля ALK, где находится профиль адаптера:

```bash
agent-lifecycle adapter launch-profile \
  --adapter codex \
  --repository-root /path/to/agent-lifecycle-kit \
  --out .alk/host-launch/codex.json
```

Для другого документированного маршрута зафиксированной задачи замените
`codex` на `claude` или `opencode`. Команда создания профиля принимает и
идентификаторы остальных встроенных адаптеров, но для них действует граница из
таблицы выше. Результат разрешено сохранять только как
`.alk/host-launch/<имя>.json`. Абсолютные и вложенные пути, выход из каталога и
символические ссылки отклоняются.

## Проверка установленной команды

Проверка структуры не создаёт процесс:

```bash
agent-lifecycle host-launch inspect \
  --profile .alk/host-launch/codex.json
```

Проверка готовности запускает один ограниченный процесс с `--version` и
создаёт рядом с профилем подтверждение, связанное отпечатком:

```bash
agent-lifecycle host-launch preflight \
  --profile .alk/host-launch/codex.json
```

Схема подтверждения: `agent-host-launch-qualification-receipt.v1`.

В подтверждении должны совпасть адаптер, отпечаток профиля, ожидаемая и
фактическая версии. Отсутствующее подтверждение, изменение профиля или версии,
превышение времени и ненулевой код завершения блокируют основной запуск до
создания процесса.

## Запуск зафиксированной задачи

Используйте проверенный профиль только с полным зафиксированным запросом режима
`implement`:

```bash
agent-lifecycle start \
  --adapter codex \
  --mode implement \
  --file work/run/adapter-run-request.json \
  --risk auto \
  --host-model-profile profiles/hosts/codex-live-profile.v1.json \
  --launch \
  --host-launch-profile .alk/host-launch/codex.json
```

В поставляемых аргументах содержатся привязка зафиксированной задачи и
разрешённые аргументы процесса. Внешний инструмент получает указание продолжить уже зафиксированную
задачу ALK в текущем проекте. Перед созданием процесса ALK повторно проверяет
файл блокировки плана, происхождение задачи, операцию, исходную редакцию и
профиль риска.

## Границы учёта и поддержки

Три обработчика умеют извлекать разрешённые счётчики из ограниченного потока
JSON. В дескрипторах для них указан статус `FIXTURE_ONLY`; приёмка S1/S2
использует отдельное подтверждение расхода от внешнего инструмента.

Профиль покрывает идентичность процесса, привязку задачи и доказательства
жизненного цикла. Вход в учётную запись, выбор поставщика, настройки внешнего
инструмента и продолжение его диалога остаются ответственностью хоста. См. [локальный запуск
внешней команды](local-host-launch.md), [использование ALK с
адаптером](../adapters/usage-modes.md), [управляемые сессии
адаптеров](managed-adapter-sessions.md) и [матрицу поддержки
адаптеров](../adapters/support-matrix.md).
