# Agent Lifecycle Kit v0.12.1 Notes

Status: source release.

## Highlights

- Fixed the Windows CI failure in the adapter scaffold CLI test by comparing
  generated scaffold paths as portable POSIX paths.
- Updated package metadata to `0.12.1`.
- Updated Codex, Claude, Cursor, Hermes and marketplace plugin metadata to
  publish the same patch version.
- Updated README, Russian README, adapter docs and support matrix descriptions
  so the current source release points at `v0.12.1`.

## Scope

This patch does not change adapter maturity. Codex CLI and Claude Code remain
the only `VERIFIED` hosts. Cursor, Gemini CLI, Hermes, Kimi Code, OpenCode, and
Qwen Code remain `EXPERIMENTAL` until live host conformance, usage calibration
and lifecycle proof evidence exist for each host.
