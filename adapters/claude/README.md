# Claude Code Adapter Projection

This directory contains the Claude Code projection for Agent Lifecycle Kit.
It is an offline release artifact that points to the shared lifecycle skills
and adapter contract instead of reimplementing controller semantics.

Live Claude Code runtime verification is a later promotion gate and is not
claimed by this EXPERIMENTAL adapter descriptor.
