# Historical Standalone Plan

`plans/standalone-v1` is preserved as historical release evidence and as a
lineage-regression fixture.

The package currently keeps a revision 16 manifest alongside revision 14
workflow and finalization artifacts. Current release-candidate verification
must reject that mixed lineage. Do not use this directory as fresh release
proof unless it is explicitly refreshed, locked, and audited in a new run.
